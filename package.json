{
  "name": "runq",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "db:generate": "turbo db:generate --filter=@runq/db",
    "db:migrate": "turbo db:migrate --filter=@runq/db",
    "db:seed": "turbo db:seed --filter=@runq/db"
  },
  "packageManager": "pnpm@10.29.3",
  "devDependencies": {
    "turbo": "^2.8.20"
  }
}
