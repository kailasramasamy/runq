// Standalone /finance/support page removed — support lives in the LifeBuoy
// drawer (see components/support/support-widget.tsx). This file is kept as
// an empty placeholder so older imports don't break; safe to delete.
export {};
